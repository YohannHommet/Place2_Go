{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div id="flash_messages" class="alert alert-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}