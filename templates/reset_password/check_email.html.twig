{% extends 'layout/base.html.twig' %}

{% block title 'Email de réinitialisation envoyé' %}

{% block body %}

	<div class="container">
		<section id="checkEmail">

			<div class="row">
				<div class="col-sm-12 col-md-10 col-lg-6 mx-auto">

					<img src="{{ asset('img/pinmap.png') }}" alt="logo" class="pinmap" draggable="false">

					<h2 class="home__title">Vérification</h2>

					<p>
						Si un compte existe avec votre email, vous recevrez un lien qui vous permettra de réinitialiser votre mot de passe.			
													        Ce lien expirera dans
						{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
					</p>
					<p>Si vous n'avez rien reçu, vérifiez votre boite de spams ou bien
						<a href="{{ path('app_forgot_password_request') }}">veuillez réessayer</a>.</p>

					<div class="d-flex justify-content-center">
						<a class="btn button-primary mt-5" href="{{ path('app_home') }}" role="button">Retour à l'accueil</a>
					</div>
				</div>
			</div>

		</section>
	</div>

{% endblock %}
