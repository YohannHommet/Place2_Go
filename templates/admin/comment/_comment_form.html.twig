{{ form_start( form, { attr: {novalidate: 'novalidate'} } ) }}

    <div class="row">
        <div class="col-md-6">
            <h3>Info</h3>
            <table class="table table-hover">
                <tbody>
                    <tr>
                        <th>Auteur</th>
                        <td><a href="{{ path('admin_user_show', {id: comment.author.id}) }}">{{ comment.author|capitalize }} ({{ comment.author.nickname|capitalize }})</a></td>
                    </tr>
                    <tr>
                        <th>Sortie</th>
                        <td><a href="{{ path('admin_event_show', {id: comment.event.id}) }}">{{ comment.event.title }}</a></td>
                    </tr>
                    <tr>
                        <th>Date</th>
                        <td>{{ comment.createdAt|date('d/m/Y H:i') }}</td>
                    </tr>
                </tbody>
            </table>     
        </div>
        <div class="col-md-6">
            <h3>Contenu</h3>
            {{ form_row(form.content) }}
        </div> 
    </div>

    <div class="my-3">
        <a class="btn btn-secondary me-2" href="{{ path('admin_comment_list') }}">
            <i class="fas fa-angle-left me-2"></i>Annuler</a>

        <button type="submit" class="btn btn-success">{{ button_label ?? 'Créer le commentaire' }}</button>
    </div>

{{ form_end(form) }}
