
{{ form_start( form, { attr: {novalidate: 'novalidate'} } ) }}

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.lastname) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.firstname) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.nickname) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.avatar) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            {{ form_row(form.birthday) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.city) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.phone) }}
        </div>
    </div>

    <!-- Divider -->
    <hr>

    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.email) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.password) }}
        </div>
    </div>

    <!-- Divider -->
    <hr>

    <div class="row">
        <div class="col-md-12">
            {{ form_row(form.description, {'attr': {'placeholder': 'Ecrivez un petit mot sur vous'}}) }}
        </div>
    </div>

    <div class="my-3">
        <a class="btn btn-secondary me-2" href="{{ path('admin_user_list') }}">
            <i class="fas fa-angle-left me-2"></i>Annuler</a>

        <button type="submit" class="btn btn-success">{{ button_label ?? 'Créer l\'utilisateur' }}</button>
    </div>

{{ form_end(form) }}
